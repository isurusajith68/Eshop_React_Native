import { StyleSheet } from "react-native";
import { COLORS, SIZES } from "../../constants";
const styles = StyleSheet.create({
  container: { marginHorizontal: 12, marginTop: SIZES.medium },
});

export default styles;
